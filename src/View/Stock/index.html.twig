{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Items</h1>
    <ul>
        {% for stock in stocks %}
            <li><a href="/stock/show/{{ stock.id }}">{{ stock.name }}. {{ stock.description }}. {{ stock.avalaibleNumber }}. {{ stock.price }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/stock/add">Ajouter</a>
{% endblock %}
