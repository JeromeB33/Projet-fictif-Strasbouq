{% extends 'layout.html.twig' %}

{% block title %} Inscription - {{ parent() }} {% endblock %}



{% block content %}
    <ul>
        {% for error in errors %}
            <li> {{ error }} </li>
        {% endfor %}

        {% if message is defined %}
            <li> {{ message }} </li>
        {% endif %}
    </ul>

    <form method="post" action="/connexion/addUser" class="uk-form-width-large uk-align-center">
        <fieldset class="uk-fieldset">
            <legend class="uk-legend">Inscription</legend>

            <div class="uk-inline">
                <input required class="uk-input"type="text"  name="firstname" placeholder="Votre nom">
            </div>
            <div class="uk-inline">
                <input required class="uk-input" type="text" name="lastname" placeholder="Votre prénom">
            </div>
            <div class="uk-inline">
                <input required class="uk-input" type="email" name="email" placeholder="Votre Email">
            </div>
            <div class="uk-inline">
                <input required class="uk-input" type="tel"  name="phone" placeholder="Votre numéro de téléphone">
            </div>
            <div class="uk-inline">
                <input required class="uk-input" type="password"  name="password" placeholder="Votre mot de passe">
            </div>
            <div class="uk-inline">
                <input class="uk-input" type="submit" value="Valider">
            </div>
        </fieldset>
    </form>
    <p class="uk-text-center">Déjà inscrit ? <a href="/Home/LogIn">Connection</a></p>



{% endblock %}