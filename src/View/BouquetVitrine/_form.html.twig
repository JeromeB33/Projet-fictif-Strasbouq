{# TODO : manage errors display #}

<form method="post">
	<input type="hidden" value="{{ bouquetVitrine.id }}" name="id">
	<input type="text" value="{{ bouquetVitrine.name }}" name="name">
	<input type="number" value="{{ bouquetVitrine.price }}" name="price">
	<input type="submit" value="Valider">
</form>
